---

interface Props {
  items?: string[];
  title?: string;
}

const { items, title } = Astro.props 


---


{items && <div>
  <h3 class="text-lg font-semibold">{title}</h3>
  <ul class="list-disc pl-5">
    {items.map((item) => (
      <li>{item}</li>
    ))}
  </ul>
</div>
}