---
interface Props {
  className?: string;
  marginBottom?: string;
  id?: string;
}

const { 
  className = "", 
  marginBottom = "mb-6",
  id
} = Astro.props;
---

<section class={`${marginBottom} ${className}`} id={id}>
  <slot />
</section> 