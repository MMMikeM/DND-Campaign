/**
 * Utility functions for the Sites module
 */

// Add site-specific utility functions here as needed
// e.g., formatting site types, encounter difficulties, etc.

export const getSiteTypeIcon = (siteType: string | null | undefined) => {
	// Placeholder - map site types to icons
	// Example:
	// switch (siteType) {
	// 	case "building": return BuildingIcon;
	// 	case "cave": return CaveIcon;
	// 	default: return DefaultLocationIcon;
	// }
	return null // Return null or a default icon
}
